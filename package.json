{
  "name": "oasis-editor",
  "private": true,
  "version": "5.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm:build": "wasm-pack build ./rust-core --target web --out-dir ../public/wasm",
    "dev:rust": "npm run wasm:build && vite",
    "build:all": "npm run wasm:build && vite build"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
